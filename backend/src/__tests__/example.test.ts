// backend/src/__tests__/example.test.ts
import { describe, expect, test } from '@jest/globals';

describe('Sample Test Suite', () => {
  test('should validate basic arithmetic', () => {
    expect(1 + 1).toBe(2);
  });
});